# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthResponse {
  accessToken: String!
  refreshToken: String!
  user: User!
}

type Comment {
  _id: ID!
  content: String!
  createdAt: DateTime!
  parentCommentId: ID
  postId: ID!
  updatedAt: DateTime!
  user: User
  userId: ID!
}

input CreateCommentInput {
  content: String!
  parentCommentId: ID
  postId: ID!
}

input CreatePostInput {
  content: String!
  published: Boolean
  title: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

input LoginDto {
  email: String!
  password: String!
}

type Mutation {
  addComment(createCommentInput: CreateCommentInput!): Comment!
  createPost(createPostInput: CreatePostInput!): Post!
  deleteComment(id: ID!): Boolean!
  deletePost(id: ID!): Boolean!
  login(loginDto: LoginDto!): AuthResponse!
  refresh(refreshToken: String!): AuthResponse!
  register(registerDto: RegisterDto!): AuthResponse!
  updateComment(id: ID!, updateCommentInput: UpdateCommentInput!): Comment!
  updatePost(id: ID!, updatePostInput: UpdatePostInput!): Post!
}

type PaginatedComments {
  currentPage: Int!
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  items: [Comment]
  limit: Int!
  totalCount: Int!
  totalPages: Int!
}

type PaginatedPosts {
  currentPage: Int!
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  items: [Post]
  limit: Int!
  totalCount: Int!
  totalPages: Int!
}

input PaginationInput {
  limit: Int! = 10
  page: Int! = 1
}

type Post {
  _id: ID!
  author: User
  authorId: ID!
  commentsCount: Int!
  content: String!
  createdAt: DateTime!
  published: Boolean!
  title: String!
  updatedAt: DateTime!
}

type Query {
  commentsByPost(paginationInput: PaginationInput, postId: ID!): PaginatedComments!
  post(id: ID!): Post!
  posts(paginationInput: PaginationInput): PaginatedPosts!
}

input RegisterDto {
  email: String!
  fullName: String!
  password: String!
}

input UpdateCommentInput {
  content: String!
}

input UpdatePostInput {
  content: String
  published: Boolean
  title: String
}

type User {
  _id: ID!
  createdAt: DateTime!
  email: String!
  fullName: String!
  updatedAt: DateTime!
}